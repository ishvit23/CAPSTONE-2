# Frontend environment configuration
# Copy to `.env` (or `.env.local`) at the project root.

# Base URL for the chatbot backend (must include the `/api` suffix)
VITE_API_BASE_URL=http://localhost:8000/api

